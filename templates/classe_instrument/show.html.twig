{% extends 'base.html.twig' %}

{% block title %}ClasseInstrument{% endblock %}

{% block body %}
    <h1>ClasseInstrument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe_instrument.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ classe_instrument.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_instrument_index') }}">back to list</a>

    <a href="{{ path('app_classe_instrument_edit', {'id': classe_instrument.id}) }}">edit</a>

    {{ include('classe_instrument/_delete_form.html.twig') }}
{% endblock %}
