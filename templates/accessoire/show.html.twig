{% extends 'base.html.twig' %}

{% block title %}Accessoire{% endblock %}

{% block body %}
    <h1>Accessoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accessoire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ accessoire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accessoire_index') }}">back to list</a>

    <a href="{{ path('app_accessoire_edit', {'id': accessoire.id}) }}">edit</a>

    {{ include('accessoire/_delete_form.html.twig') }}
{% endblock %}
