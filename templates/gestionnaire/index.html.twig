{% extends 'base.html.twig' %}

{% block title %}Gestionnaire index{% endblock %}

{% block body %}
    <h1>Gestionnaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gestionnaire in gestionnaires %}
            <tr>
                <td>{{ gestionnaire.id }}</td>
                <td>{{ gestionnaire.email }}</td>
                <td>{{ gestionnaire.password }}</td>
                <td>
                    <a href="{{ path('app_gestionnaire_show', {'id': gestionnaire.id}) }}">show</a>
                    <a href="{{ path('app_gestionnaire_edit', {'id': gestionnaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_gestionnaire_new') }}">Create new</a>
{% endblock %}
