{% extends 'base.html.twig' %}

{% block title %}Tranche{% endblock %}

{% block body %}
    <h1>Tranche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tranche.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ tranche.libelle }}</td>
            </tr>
            <tr>
                <th>QuotientMini</th>
                <td>{{ tranche.quotientMini }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tranche_index') }}">back to list</a>

    <a href="{{ path('app_tranche_edit', {'id': tranche.id}) }}">edit</a>

    {{ include('tranche/_delete_form.html.twig') }}
{% endblock %}
