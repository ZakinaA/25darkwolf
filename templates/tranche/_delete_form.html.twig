<form method="post" action="{{ path('app_tranche_delete', {'id': tranche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tranche.id) }}">
    <button class="btn">Delete</button>
</form>
