{% extends 'base.html.twig' %}

{% block title %}Accueil e-Music{% endblock %}

{% block body %}
<style>
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f6f8;
    display: flex;
}

.sidebar {
    width: 240px;
    background: linear-gradient(180deg, #1f3d7a 0%, #3456a1 100%);
    min-height: 100vh;
    padding: 25px 20px;
    color: #fff;
    display: flex;
    flex-direction: column;
    box-shadow: 2px 0 10px rgba(0,0,0,0.1);
}

.sidebar h2 {
    margin-top: 0;
    font-size: 1.8em;
    margin-bottom: 30px;
    text-align: center;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
}

.sidebar a {
    color: #fff;
    text-decoration: none;
    padding: 12px 15px;
    display: flex;
    align-items: center;
    font-weight: 500;
    border-radius: 6px;
    margin-bottom: 12px;
    transition: all 0.3s ease;
}

.sidebar a:hover {
    background-color: #4a6fc1;
    transform: translateX(4px);
}

.sidebar a i {
    margin-right: 10px;
}

.main-content {
    flex-grow: 1;
    padding: 50px 60px;
}

h1 {
    color: #1f3d7a;
}
</style>

<div class="sidebar">
    <h2>e-Music</h2>
    <a href="{{ path('app_contrat_pret') }}"><i>📋</i> Lister les prêts</a>
    <a href="#"><i>🎻</i> Instruments</a>
    <a href="#"><i>👥</i> Élèves</a>
    <a href="#"><i>🧑‍🏫</i> Professeurs</a>
    <a href="#"><i>📅</i> Planning</a>
    <a href="#"><i>⚙️</i> Paramètres</a>
</div>

<div class="main-content">
    <h1>Bienvenue sur l'application e-Music !</h1>
    <p>Utilisez la barre de navigation à gauche pour accéder aux différentes sections.</p>
</div>
{% endblock %}
