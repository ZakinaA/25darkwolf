{% extends 'base.html.twig' %}

{% block title %}Dashboard e-Music{% endblock %}

{% block body %}

<div class="sidebar">
    <h2>e-Music</h2>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="#"><i>📋</i> Gérer les prêts</a>
        <a href="#"><i>🎻</i> Instruments</a>
        <a href="#"><i>👥</i> Élèves</a>
        <a href="#"><i>🧑‍🏫</i> Professeurs</a>
        <a href="#"><i>⚙️</i> Paramètres</a>

    {% elseif is_granted('ROLE_TEACHER') %}
        <a href="#"><i>📅</i> Mon planning</a>
        <a href="#"><i>👥</i> Mes élèves</a>
        <a href="#"><i>🎻</i> Instruments à gérer</a>

    {% else %}
        <a href="#"><i>📅</i> Mon emploi du temps</a>
        <a href="#"><i>🎻</i> Mes instruments</a>
        <a href="#"><i>🔔</i> Notifications</a>
    {% endif %}
</div>

<div class="main-content">
    {% if is_granted('ROLE_ADMIN') %}
        <h1>Bienvenue Admin {{ user.username }} !</h1>
        <p>Vous pouvez gérer les instruments, élèves et professeurs.</p>

    {% elseif is_granted('ROLE_TEACHER') %}
        <h1>Bienvenue Professeur {{ user.username }} !</h1>
        <p>Voici vos cours et vos élèves.</p>

    {% else %}
        <h1>Bienvenue Élève {{ user.username }} !</h1>
        <p>Consultez votre planning et vos instruments.</p>
    {% endif %}
</div>
{% endblock %}
